// Javascript
/*
$(document).ready(function () {
    console.log("ready");
    var news_list = [];
    var counter = 0;

    // formats datetime stored in ID
    $("li").each(function() {
        $(this).attr('id');
    });

    // populates news_list with elements parsed in HTML.
    $('ul li').each(function() {news_list.push($(this).html());});


    $("#pubdate").click(function () {
        //while (counter <= news_list.length - 1){counter += 1};
        // debug
        console.log(news_list);
        console.log(counter);
    });
});

function format_date (date_id){

};
*/